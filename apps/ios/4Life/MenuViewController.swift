//
//  MenuViewController.swift
//  4Life
//
//  Created by jmWork on 19/12/14.
//  Copyright (c) 2014 Liferay. All rights reserved.
//

import UIKit

class MenuViewController: UIViewController {

	override func viewDidLoad() {
/*
		let b = UIImageView(image: UIImage(named: "menu-back"))
		b.setTranslatesAutoresizingMaskIntoConstraints(false)
		b.frame = CGRect(
				x: 0, y: 0,
				width: UIScreen.mainScreen().bounds.size.width + 589.0,
				height: UIScreen.mainScreen().bounds.size.height)
		b.contentMode = UIViewContentMode.ScaleAspectFit

		view.addSubview(b)
		view.sendSubviewToBack(b)

		let dict = ["imageView": b] as [NSObject : AnyObject]
		var c = NSLayoutConstraint.constraintsWithVisualFormat("H:|-0-[imageView]",
						options: NSLayoutFormatOptions.allZeros,
						metrics: nil,
						views: dict)
		view.addConstraints(c)

		c = NSLayoutConstraint.constraintsWithVisualFormat("V:|-0-[imageView]",
						options: NSLayoutFormatOptions.allZeros,
						metrics: nil,
						views: dict)
		view.addConstraints(c)
*/
}

}
